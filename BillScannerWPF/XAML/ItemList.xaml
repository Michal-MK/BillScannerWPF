<UserControl x:Class="Igor.BillScanner.WPF.UI.ItemList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Igor.BillScanner.WPF.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.Background>
			<SolidColorBrush/>
		</Grid.Background>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="20"></ColumnDefinition>
			<ColumnDefinition Width="*"></ColumnDefinition>
			<ColumnDefinition Width="20"></ColumnDefinition>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="20"></RowDefinition>
			<RowDefinition Height="*"></RowDefinition>
			<RowDefinition Height="20"></RowDefinition>
		</Grid.RowDefinitions>
		
		<Grid Grid.Row="1" Grid.Column="1" Background="WhiteSmoke">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="10"></ColumnDefinition>
				<ColumnDefinition Width="*"></ColumnDefinition>
				<ColumnDefinition Width="10"></ColumnDefinition>
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition Height="10"></RowDefinition>
				<RowDefinition Height="*"></RowDefinition>
				<RowDefinition Height="64"></RowDefinition>
			</Grid.RowDefinitions>
			
			<ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">
				<ListBox x:Name="ITEMLIST_Items_ListBox" HorizontalContentAlignment="Stretch" ItemsSource="{Binding Items}" SelectionChanged="ITEMLIST_Items_ListBox_SelectionChanged"/>
			</ScrollViewer>

			<StackPanel Grid.Row="2" Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right" Orientation="Horizontal">
				<TextBlock	HorizontalAlignment="Left" x:Name="ITEMLIST_ErrorInfo_Text" Foreground="Red"></TextBlock>
				<Button	HorizontalAlignment="Right" Command="{Binding SelectCommand}" Content="Select" Margin="0,10,10,10" Padding="20,5"></Button>
				<Button	HorizontalAlignment="Right" Command="{Binding AbortCommand}" Content="Back" Margin="0,10,10,10" Padding="20,5"></Button>
			</StackPanel>
		</Grid>
	</Grid>
</UserControl>
